import { NotFoundPage } from '@/components/ui/error';

export default function NotFound() {
  return <NotFoundPage />;
}

export const viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
};
